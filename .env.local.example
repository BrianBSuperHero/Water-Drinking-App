# Copy this to `.env.local` for local development (if using a build tool).
# For this static app we include keys directly in `lib/supabase.js` — you can replace them there.

SUPABASE_URL=https://zytgakxqbegjmdqdbisw.supabase.co
SUPABASE_ANON_KEY=sb_publishable_XXXXXXXXXXXX

# Do NOT commit your service_role key. Use only the anon/public key on the client.
