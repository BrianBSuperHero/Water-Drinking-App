/*
  Copy this file to `env.js` in the project root and fill in your Supabase
  Project URL and anon/public key. Keep `env.js` out of source control.

  Example usage (env.js):
    window.SUPABASE_URL = 'https://your-project-ref.supabase.co';
    window.SUPABASE_ANON_KEY = 'sb_public_XXXXXXXX...';

  For static local development this makes the values available as globals
  before the app's module code runs. Do NOT commit real keys to public repos.
*/

// Paste your values below when copying to `env.js`
window.SUPABASE_URL = 'https://your-project-ref.supabase.co';
window.SUPABASE_ANON_KEY = 'sb_public_YOUR_ANON_KEY_HERE';
